package tasks

import (
	// "data// base/sql"
	// "fmt"
	// "github.com/Defake/day-assistant/data_access/db"
	// "log"
)



func SaveTask() {
	// TODO

	// email := "admin@test.com"
	// rows, err := db.Connection.Query("SELECT body->>'name' FROM users WHERE body->>'email' = $1", email)
	// if err != nil {
	// 	log.Fatal(err)
	// }
	// defer rows.Close()

	// var (
	// 	name string
	// )

	// for rows.Next() {
	// 	err := rows.Scan(&name)
	// 	if err != nil {
	// 		log.Fatal(err)
	// 	}
	// 	log.Println(name)
	// }

	// err = rows.Err()
	// if err != nil {
	// 	log.Fatal(err)
	// }
}
